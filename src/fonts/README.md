# LOH brand fonts — committed in git (push-safe).
# Loaded via `next/font/local` in `src/lib/lohLocalFonts.ts` (bundled into the app).
# Do not rely on Bunny CDN alone for these faces: many .otf files lack CORS and fail in the browser.
